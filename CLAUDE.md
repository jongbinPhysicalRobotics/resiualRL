# 작업 규칙 (Claude Code)

학부 졸업 논문: **Unitree G1 (MuJoCo) 의 Di Carlo 식 convex MPC (SRB wrench MPC) + residual RL 보행 제어기.**
2026-09-25 Windows 노트북에서 Ubuntu 로 옮기며, 그동안 쌓인 규칙을 여기 모았다. 경위는 `Q&A/` 날짜 파일.

## 작업 범위
- 이 저장소 폴더 안에서만 작업한다 (사용자가 권한을 모두 준 대신 조건으로 건 것).
- 커밋·푸시는 Q&A 한 항목을 쓸 때마다 한다 (지금까지의 관례).

## 기록
- 개념 설명·실험 결과는 `Q&A/<YYYY-MM-DD>.md` 에 Q 번호로 쌓고 `Q&A/README.md` 색인에 한 줄 추가한다.
  각 항목은 자기완결적으로, 측정한 숫자로, 코드는 상대 링크로.
- 틀린 말을 발견하면 원래 항목에 "⚠ 정정 (Qn)" 을 달고 새 항목에서 바로잡는다 (지우지 않는다).
- **주장마다 근거의 종류를 밝힌다**: 코드 사실 / 측정 / 추론 / 근거 없는 추측. 추론을 사실처럼, 코드 대조를 실행 검증처럼 쓰지 않는다
  (9/25 Q3·Q4: "데모는 키보드로 사람이 몰았다", "warm start 때문에 원점 고정", "이식 오류 아님" 을 과하게 말했다가 정정).

## 판정 기준
- 완주 판정은 **120 s** (20 s 통과가 완주가 아니었다 — 발 꼬임 발산).
- 채택은 추종 % 만 보고 하지 않는다 — `18_gait_quality.py` 표 (무릎·스윙 모음·발 미끄럼·골반 yaw 등) + **렌더**로 걸음 모양까지 본다.
- 1 m/s 까지는 **MPC 단독**으로 가는 것이 목표. "residual RL 이 메운다" 는 식으로 틈을 넘기지 않는다.

## MPC 설계 원칙
- MPC 모델은 **Di Carlo SRB convex MPC 틀**을 유지한다 (상태 13 · 입력 12 · 볼록 QP 1 회). IK·관절 자세·전신 관성·CMM 을 MPC 모델에 넣지 않는다.
  확장은 간단한 모델로 미리 계산한 알려진 외란을 중력과 같은 아핀 통로로 넣는 것까지 (affine, Q&A 9/23 ~ 9/24).
- 회전 각속도는 **축마다 전신 아니면 골반 하나** — 섞기 (0.5) 금지. 모델 값 (관성 등) 은 그 정의에서 유도한 대로 쓰고,
  유도값이 실험상 나쁘면 cost 로 억제 → 그래도 안 되면 원인 규명. 유도와 다른 값을 쓰면 반드시 "근사" 로 밝힌다.
  현재 선택: H 안 (roll·pitch 전신, yaw 골반) + affine + 골반 정지 참조, yaw 각속도 식은 I_전신 근사 (Q&A 9/24 Q35 ~ Q37).
- 새 기능은 **플래그로 추가, 기본값 = 기존 동작**. A/B 로 비교한 뒤 채택.

## 플랫폼
- 로봇은 **G1 우선**. MIT 휴머노이드는 병행 트랙 (`MPC/src/mit`, reference C++ 이식 · 대조군) — 전환은 사용자가 G1 한계를 판단할 때만.
- 코드 폴더 안내는 `MPC/src/README.md`. 각 폴더는 자기 파일만으로 실행된다 (공용 모듈은 복사본 — 한 폴더를 고쳐도 다른 폴더엔 안 들어간다).
- `gait.py` 의 `GAIT_ON` 은 환경변수 `GAIT_ON=1` 이 파일 값보다 우선 (affine 폴더 파일 값은 0 = 서 있기).

## 환경
- Ubuntu: `.venv/bin/python`, `requirements.txt` (SETUP.md). Windows 에서 쓰던 `PYTHONIOENCODING=utf-8` · 한글 경로 VFS 우회는 Ubuntu 에선 필요 없다
  (VFS 로더는 그대로 둬도 동작한다).
- 병렬 실행은 `multiprocessing.Pool`. MIT 폴더는 BLAS 스레드를 1 로 고정해야 빠르다 (`OPENBLAS_NUM_THREADS=1`, 러너가 설정).
- 옮긴 직후 대표 표를 다시 돌려 수치 기준을 새로 잡는다 (SETUP.md "옮긴 뒤 확인할 것").
