# ── 파이썬 가상환경 ────────────────────────────────────────────
# 절대경로가 박혀 있어 다른 PC 에서 깨진다. 새 PC 에서 새로 만들 것 (SETUP.md).
.venv/
__pycache__/
*.pyc

# ── 외부 git 저장소 (clone 으로 받는다 — SETUP.md) ─────────────
mujoco_menagerie/
reference/
deployed RL/unitree_rl_gym/

# ── 실험 로그 (재생성 가능, 429 MB) — PNG 플롯만 포함 ─────────
MPC/logs/*.npz
MPC/logs/*.csv

# ── 기계별 설정 (한글 절대경로가 들어감) ──────────────────────
.vscode/

# ── 기타 ──────────────────────────────────────────────────────
*.tmp
.DS_Store
Thumbs.db
